# AI用開発ガイド & プロジェクト概要 (later-cli)

本ドキュメントは、AIアシスタントがこの `later-cli` プロジェクトを迅速に理解し、効率的に追加開発やデバッグを行うためのポイントをまとめたガイドです。

---

## 1. プロジェクト概要
`later-cli` は、コマンドライン（CLI）から簡単な入力で期限付きのタスクを登録・管理するための Python アプリケーションです。

- **主要な目的**: 「明日」「3日後」「10時間後」といった直感的な表現でタスクを追加し、期限が来たタスクを表示したり、一括削除したりする。
- **データ永続化**: JSON ファイル（デフォルトは `tasks.json`）にタスク一覧を保存・管理。

---

## 2. 技術スタック
プロジェクトで使用されている主な技術とツールは以下の通りです。

| カテゴリ | ツール / パッケージ | バージョン | 用途 |
| :--- | :--- | :--- | :--- |
| **言語** | Python | 3.x | メイン言語 |
| **パッケージマネージャー** | [uv](https://github.com/astral-sh/uv) | `0.7.8` | 高速な依存関係解決、仮想環境管理、およびロックファイルの管理 |
| **CLIフレームワーク** | [Typer](https://typer.tiangolo.com/) | `0.25.1` | CLIコマンドの作成、引数・オプションのパース |
| **画面出力** | [Rich](https://rich.readthedocs.io/) | `15.0.0` | テーブル表示やカラー出力などのリッチな端末表示 |
| **テスト** | [pytest](https://docs.pytest.org/) | `9.0.3` | 単体テストおよび統合テストの実行 |
| **Linter / Formatter** | [ruff](https://astral.sh/ruff), [black](https://github.com/psf/black) | `0.9.2` / `24.10.0` | コードの静的解析と自動整形 |
| **タスクランナー** | [just](https://github.com/casey/just) | - | 開発コマンドの統合管理 |

---

## 3. ディレクトリ・ファイル構成と役割

プロジェクトは非常にシンプルかつモジュール化された構成になっています。

```text
later-cli/
├── later.py         # ★ メインエントリーポイント。CLIコマンド定義と期限の計算処理。
├── storage.py       # タスクデータの読み込み、保存、ソート、保存先管理。
├── later            # later.py を呼び出すための bash ラッパースクリプト。
├── tasks.json       # タスクデータが保存されるJSONファイル (デフォルト)。
├── requirements.txt # Python依存パッケージの定義（旧構成）。
├── pyproject.toml   # ★ uv / PEP621準拠のプロジェクト・依存関係設定ファイル。
├── uv.lock          # uvによる依存関係のロックファイル。
├── justfile         # 開発用タスクランナー (just) の設定 (uv run対応済み)。
├── tests/
│   └── test_cli.py  # TyperのCliRunnerを使用したコマンドテスト。
└── README.md        # ユーザー向けの使用方法やセットアップ手順。
```

---

## 4. AI開発者が把握すべきコード仕様・仕様ルール

### 4.1. データ構造
タスク情報は `list[dict]` 構造で管理され、JSON に保存されます。データは常に**期限（`date`）の昇順**でソートされて保存・読み込みされます。
```json
[
    {
        "date": "2026-05-30 08:00:00",
        "task": "レポート提出"
    }
]
```

### 4.2. 期限（Due Date）のパースルール (`calc_due_date`)
`later.py` に実装されている `calc_due_date(due: str)` は、以下のような表現をパースして `datetime` オブジェクトに変換します。

- **即時**: `now` / `すぐ` / `今` $\rightarrow$ 現在日時そのもの
- **明日**: `明日` / `tomorrow` $\rightarrow$ 翌日の午前8時（`08:00:00`）
- **明後日**: `明後日` $\rightarrow$ 2日後の午前8時（`08:00:00`）
- **来週**: `来週` / `nextweek` $\rightarrow$ 7日後の午前8時（`08:00:00`）
- **相対時間（日単位）**: `(\d+)d` または `(\d+)日` $\rightarrow$ N日後の午前8時（`08:00:00`）
- **相対時間（時間単位）**: `(\d+)h` $\rightarrow$ N時間後の現在時刻からの相対時間

> [!IMPORTANT]
> **「日付」単位の指定（明日、来週、d単位など）は、すべて時間情報が「午前8時 (08:00:00)」にリセット（正規化）される点に注意してください。**

### 4.3. コマンド仕様
- `add` (エイリアス `a`): タスクの追加。
- `show`: タスク一覧を `rich.table.Table` で表示。**「残り」カラムが追加され、期限までのカウントダウン（日数・時間・分、または赤字の「超過」表記）がリアルタイムに計算されて表示されます。**
- `delete`: 1から始まるインデックス（画面上の番号）を指定してタスクを削除。
- `clear`: 期限が1日以上過ぎたタスク（`notify_at - 1日 > 現在時刻`）を一括削除。
- `check`: 期限が現在時刻以前（`notify_at <= now`）のタスクのみを表示。
- `info`: タスクが保存されているJSONファイルの絶対パスを表示。

---

## 5. 起点となるファイル (Entry Point)

Antigravity（AIアシスタント）や他の開発者がこのプロジェクトを扱う上で、**「起点となるファイル」**は用途に応じて以下の通りです。

### 1) プログラムとしての実行起点 (Application Entry Point)
- **[later.py](./later.py)**
  - TyperのCLIアプリケーション定義（`app = typer.Typer(...)`）があり、すべてのサブコマンド（`add`, `show`, `delete` 等）やメインロジックがここに実装されています。直接Pythonで動かす際のエントリーポイントです。
- **[later](./later)**
  - ユーザーがパスを通して `later add ...` のように簡単に実行できるようにするための bash ラッパースクリプトです。**.venv ディレクトリが存在し、かつ `uv` コマンドがインストールされている場合は自動的に `uv run` 経由で実行され、それ以外の場合は従来の `python` 経由で安全にフォールバック実行されます。**

### 2) プロジェクト理解・ドキュメントの起点 (Information Entry Point)
- **[README.md](./README.md)**
  - プロジェクトの概要、インストール手順、および各コマンドの具体的な使用方法が日本語で美しくまとまっています。仕様変更や動作確認を行う際に一番に参照すべき情報源です。

### 3) 開発・検証プロセスの操作起点 (Developer Interface)
- **[justfile](./justfile)**
  - テスト、Linter、Formatterといった開発作業のコマンドがすべてここに集約されています。AIがコードを修正した後の品質チェックの起点となるファイルです。

---

## 6. 開発時に役立つ `just` コマンド一覧

AIがプログラムの変更や検証を行う際は、プロジェクトのルートディレクトリで以下のコマンドを実行してください。これらは内部で `uv` を使用して自動的に仮想環境を構築・利用して実行されます（例: `uv run pytest`）。

```bash
# 依存関係（pytest, black, ruff等）のインストール（uv sync）
just install

# 単体テストの実行（uv run pytest）
just test

# 静的解析チェックの実行（uv run ruff check .）
just lint

# コードの自動整形（uv run black . / uv run ruff format .）
just format
```
